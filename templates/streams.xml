<?xml version="1.0" encoding="UTF-8"?>
<Response>
  <Connect>
    <Stream url="{{ url }}"></Stream>
  </Connect>
  <Response>
    <Record timeout="10"
      transcribe="true"
      recordingStatusCallback="{{ callback_url }}"
    />
  </Response>
</Response>
